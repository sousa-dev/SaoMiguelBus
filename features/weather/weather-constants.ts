/**
 * Fallback parish (Ponta Delgada city centre) used by the home weather card when
 * there is no pinned parish and device location is unavailable/denied.
 * Must exist in `GET /api/v3/weather/parishes`.
 */
export const DEFAULT_PARISH_SLUG = 'sao-sebastiao-ponta-delgada';
