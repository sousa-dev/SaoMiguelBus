/** Bottom dock height for the live vehicle stop list (map stays visible above). */
export const LIVE_VEHICLE_SHEET_HEIGHT_RATIO = 0.32;

/** Estimated row height for scrolling the ETA list to the active stop. */
export const LIVE_ETA_ROW_HEIGHT = 42;

/** Fixed height of the live fleet picker bar at the screen bottom. */
export const LIVE_FLEET_BAR_HEIGHT = 88;

/** Max scroll height for the vertical live fleet list (overview mode). */
export const LIVE_FLEET_BAR_MAX_HEIGHT = 148;

/** Max scroll height when a bus is focused (compact fleet picker). */
export const LIVE_FLEET_BAR_COMPACT_MAX_HEIGHT = 96;

/** Map bottom inset when the expanded fleet list is visible (overview refit). */
export const LIVE_FLEET_BAR_OVERVIEW_BOTTOM_INSET = LIVE_FLEET_BAR_MAX_HEIGHT + 56;
